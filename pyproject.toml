[project]
name = "collectionflow-demo"
version = "0.1.0"
description = "CollectionFlow pattern demo: XML → Polars → Parquet → JSON"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandera>=0.29.0",
    "polars>=1.38.1",
]
